worker_processes 1;

events {
    worker_connections 1024;
}

http {
    sendfile on;
    client_max_body_size 50M;

    upstream api_gateway {
        server host.docker.internal:3001;
    }

    upstream communication_service {
        server host.docker.internal:3005;
    }

    upstream notification_service {
        server host.docker.internal:3006;
    }

    server {
        listen 5000;

        # WebSocket — bypasses gateway, goes direct to communication service
        location /socket.io/ {
            proxy_pass http://communication_service/socket.io/;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Authorization $http_authorization;
        }

        # SSE — bypasses gateway, goes direct to notification service
        location /notifications/api/notifications/stream {
            proxy_pass http://notification_service/api/notifications/stream;
            proxy_http_version 1.1;
            proxy_set_header Connection '';
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 86400s;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        }

        # Everything else goes through the API gateway
        location / {
            proxy_pass http://api_gateway;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Authorization $http_authorization;
        }
    }
}
